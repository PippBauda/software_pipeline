name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      # fix #14 — pinned to full SHA for supply-chain security
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js ${{ matrix.node-version }}
        # fix #14 — pinned to full SHA
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: ${{ matrix.node-version }}

      # fix #4 — deterministic install from lockfile
      - name: Install dependencies
        run: npm ci
        working-directory: .tooling

      # Symlink node_modules to root so eslint.config.js ESM imports resolve correctly.
      # Required because NODE_PATH is not used for ES module resolution in Node.js.
      - name: Link node_modules for eslint config
        run: ln -s .tooling/node_modules node_modules

      - name: Lint
        run: npx --prefix .tooling eslint .

      - name: Check formatting
        run: npm run format:check
        working-directory: .tooling

      - name: Run tests
        run: npm test
        working-directory: .tooling
